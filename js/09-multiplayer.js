/* IN-SECT — multijoueur 1v1 : invitation + matchmaking automatique. */
(function(){
'use strict';
const MP=window.INSECT_MP={active:false,api:String(window.INSECT_MULTIPLAYER_API||'').trim(),code:null,secret:null,role:null,localColor:null,lastVersion:-1,spEnabled:null,applyingRemote:false,gameTimer:null,queueTimer:null,queueSecret:null,pushInFlight:false,lastPushedTurn:null};
const enabled=()=>/^https:\/\//i.test(MP.api);
const norm=v=>String(v||'').toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6);
function track(n,p={}){try{if(typeof gaTrack==='function')gaTrack(n,{multiplayer:true,...p})}catch(_){}}
async function api(action,payload={}){const r=await fetch(MP.api,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...payload}),cache:'no-store'});let d=null;try{d=await r.json()}catch(_){}if(!r.ok||!d||d.ok===false)throw new Error(d?.error||`Erreur serveur (${r.status})`);return d}
function status(m,err=false){const e=document.getElementById('mp-status');if(e){e.textContent=m||'';e.style.color=err?'#FF6680':'#A9A3D6'}}
function show(){document.getElementById('mp-overlay')?.classList.remove('hidden')}
function hide(){document.getElementById('mp-overlay')?.classList.add('hidden')}
function stopGamePoll(){if(MP.gameTimer)clearInterval(MP.gameTimer);MP.gameTimer=null}
function stopQueue(){if(MP.queueTimer)clearInterval(MP.queueTimer);MP.queueTimer=null}
function reset(){stopGamePoll();stopQueue();Object.assign(MP,{active:false,code:null,secret:null,role:null,localColor:null,lastVersion:-1,spEnabled:null,applyingRemote:false,queueSecret:null,pushInFlight:false,lastPushedTurn:null})}
function serialize(){if(!G||!G.players)return null;const safe=JSON.parse(JSON.stringify({...G,board:null,sel:null,pendCorpse:null,pendDisp:null,afterCorpse:null,repTargets:[]}));return{schema:1,G:safe,mode:1,aiLevel:_aiLevel,selColor:_selColor,optSP:!!MP.spEnabled,uid:_uid,turns:_gameTurns,caps:_gameCaps,moveLog:_moveLog||[],sentAt:Date.now()}}
function turnKey(){if(!G)return null;return`${Number(G.turn||0)}:${Number(G.idx||0)}:${Number(_gameTurns||0)}`}
function rebuild(){G.board=Array.from({length:9},()=>Array(9).fill(null));for(const c of Object.keys(G.players||{}))for(const p of G.players[c]?.pieces||[])if(Number.isInteger(p.r)&&Number.isInteger(p.c)&&p.r>=0&&p.r<9&&p.c>=0&&p.c<9)if(!p.dead||G.board[p.r][p.c]==null)G.board[p.r][p.c]=p}
function badge(){if(!MP.active)return;const b=document.getElementById('mode-badge');if(b)b.textContent=`EN LIGNE · ${MP.code}${MP.spEnabled?' · ⚡ SP':''}`}
function applyState(s){if(!s?.G)return;MP.applyingRemote=true;try{_mode=1;_aiLevel=s.aiLevel||1;_selColor=MP.localColor;_optSP=!!s.optSP;_uid=Number(s.uid||0);_gameTurns=Number(s.turns||0);_gameCaps=Number(s.caps||0);_moveLog=Array.isArray(s.moveLog)?s.moveLog:[];G=s.G;G.human=MP.localColor;G.mode1=true;G.sel=null;G.phase='select';G.spPaused=false;for(const c of Object.keys(G.players||{}))G.players[c].human=true;rebuild();MP.lastPushedTurn=turnKey();showScreen('game');buildBoard();renderBoard();renderPlayers();updateTurnUI();updateToggleUI();const bz=document.getElementById('bottom-zone');if(bz)bz.style.display='flex';badge()}finally{MP.applyingRemote=false}}
async function push(force=false){if(!MP.active||MP.applyingRemote||!G||MP.pushInFlight)return false;const key=turnKey();if(!force&&key&&key===MP.lastPushedTurn)return true;const state=serialize();if(!state)return false;MP.pushInFlight=true;try{const o=await api('push',{code:MP.code,secret:MP.secret,state});MP.lastVersion=Number(o.version);MP.lastPushedTurn=key;status(`Synchronisé · version ${MP.lastVersion}`);track('mp_turn_synced',{version:MP.lastVersion});return true}catch(e){console.warn('[IN-SECT MP] push',e);status(`Synchronisation impossible : ${e.message}`,true);return false}finally{MP.pushInFlight=false}}
function voteUI(o){const box=document.getElementById('mp-vote');if(!box)return;box.style.display='block';if(o?.sp_decided){MP.spEnabled=!!o.sp_enabled;box.innerHTML=`<div class="mp-result">${o.sp_random?'🎲 DÉSACCORD — LE HASARD A DÉCIDÉ':'🤝 ACCORD DES DEUX JOUEURS'}<br><b>SUPER POUVOIRS ${MP.spEnabled?'ACTIVÉS ⚡':'DÉSACTIVÉS'}</b></div>`;return}const mine=MP.role==='host'?o?.host_voted:o?.guest_voted,other=MP.role==='host'?o?.guest_voted:o?.host_voted;box.innerHTML=`<div class="mp-vtitle">⚡ SUPER POUVOIRS ?</div><div class="mp-vsub">Chacun vote. En cas de désaccord, tirage serveur 50/50.</div><div class="mp-vbuttons"><button ${mine?'disabled':''} onclick="INSECT_MP.vote(true)">OUI ⚡</button><button ${mine?'disabled':''} onclick="INSECT_MP.vote(false)">NON</button></div><div class="mp-vstate">${mine?'✓ Votre vote':'À vous de voter'} · ${other?'✓ Adversaire a voté':'En attente du vote adverse'}</div>`}
async function startHost(){if(MP.active)return;MP.active=true;MP.localColor='yellow';_mode=1;_selColor='yellow';_aiLevel=1;_optSP=!!MP.spEnabled;startGame();_optSP=!!MP.spEnabled;G.human='yellow';if(G.players.yellow)G.players.yellow.human=true;if(G.players.red)G.players.red.human=true;badge();await push(true);hide();track('mp_match_start',{source:'online',super_powers:!!MP.spEnabled})}
async function gamePoll(){if(!MP.code||!MP.secret)return;try{if(MP.active&&G&&!MP.applyingRemote&&cur()!==MP.localColor&&turnKey()!==MP.lastPushedTurn)await push();const o=await api('get',{code:MP.code,secret:MP.secret,since:MP.lastVersion});voteUI(o);if(o.status==='waiting'){status('En attente de votre adversaire…');return}if(!o.sp_decided){status('Votez pour les super pouvoirs.');return}MP.spEnabled=!!o.sp_enabled;if(MP.role==='host'&&!MP.active){status('Adversaire trouvé. Démarrage…');await startHost();return}if(o.state&&Number(o.version)>MP.lastVersion){MP.lastVersion=Number(o.version);MP.active=true;applyState(o.state);hide()}if(o.status==='finished'){MP.active=false;stopGamePoll()}}catch(e){console.warn('[IN-SECT MP] poll',e)}}
function startGamePoll(){stopGamePoll();gamePoll();MP.gameTimer=setInterval(gamePoll,1000)}
async function vote(v){try{const o=await api('vote_sp',{code:MP.code,secret:MP.secret,enabled:!!v});voteUI(o);track('mp_sp_vote',{vote:!!v});await gamePoll()}catch(e){status(e.message,true)}}
function adopt(o){MP.code=norm(o.code);MP.secret=o.secret;MP.role=o.role;MP.localColor=o.role==='host'?'yellow':'red';MP.lastVersion=-1;MP.spEnabled=null;MP.lastPushedTurn=null;stopQueue();renderMatched();startGamePoll();track('mp_match_found',{role:MP.role})}
function renderMatched(){const s=document.getElementById('mp-search');if(s)s.innerHTML=`<div class="mp-result">✅ ADVERSAIRE TROUVÉ<br><b>Vous êtes la colonie ${MP.localColor==='yellow'?'JAUNE':'ROUGE'}</b></div>`;status('Adversaire trouvé. Votez pour les super pouvoirs.')}
async function findOpponent(){if(MP.queueSecret)return;status('Recherche d’un adversaire…');const s=document.getElementById('mp-search');if(s)s.innerHTML='<div class="mp-searching">🔎 Recherche en cours…<br><span>Vous pouvez annuler à tout moment.</span></div><button class="btn-secondary" style="margin-top:9px" onclick="INSECT_MP.cancelSearch()">ANNULER</button>';try{const o=await api('matchmake_start');MP.queueSecret=o.queue_secret;track('mp_search_start');if(o.matched)return adopt(o);MP.queueTimer=setInterval(searchStatus,1000)}catch(e){MP.queueSecret=null;status(e.message,true)}}
async function searchStatus(){if(!MP.queueSecret)return;try{const o=await api('matchmake_status',{queue_secret:MP.queueSecret});if(o.matched)return adopt(o);if(o.expired){stopQueue();MP.queueSecret=null;status('Aucun adversaire trouvé pour le moment. Réessayez.',true);const s=document.getElementById('mp-search');if(s)s.innerHTML=''}}catch(e){console.warn('[IN-SECT MP] matchmaking',e)}}
async function cancelSearch(){if(!MP.queueSecret)return;try{const o=await api('matchmake_cancel',{queue_secret:MP.queueSecret});if(o.matched){const s=await api('matchmake_status',{queue_secret:MP.queueSecret});if(s.matched)return adopt(s)}}catch(_){}stopQueue();MP.queueSecret=null;const s=document.getElementById('mp-search');if(s)s.innerHTML='';status('Recherche annulée.')}
async function create(){cancelSearch();status('Création de la partie…');try{const o=await api('create');MP.code=norm(o.code);MP.secret=o.secret;MP.role='host';MP.localColor='yellow';MP.lastVersion=-1;MP.spEnabled=null;MP.lastPushedTurn=null;invite();status('Partie créée. Envoyez le lien à votre adversaire.');startGamePoll();track('mp_room_created')}catch(e){status(e.message,true)}}
async function join(v){cancelSearch();const code=norm(v);if(code.length!==6)return status('Code invalide.',true);status('Connexion…');try{const o=await api('join',{code});MP.code=code;MP.secret=o.secret;MP.role='guest';MP.localColor='red';MP.lastVersion=-1;MP.spEnabled=null;MP.lastPushedTurn=null;status('Connecté. Votez pour les super pouvoirs.');startGamePoll();track('mp_room_joined')}catch(e){status(e.message,true)}}
function joinUrl(){const u=new URL(location.href);u.searchParams.set('join',MP.code);u.hash='';return u.toString()}
async function copy(){try{await navigator.clipboard.writeText(joinUrl());status('Lien copié. Envoyez-le à votre adversaire.')}catch(_){window.prompt('Copiez ce lien :',joinUrl())}}
async function share(){try{if(navigator.share)await navigator.share({title:'Défi IN-SECT',text:`⚔️ Je te défie sur IN-SECT. Code ${MP.code}`,url:joinUrl()});else await copy();track('mp_invite_shared')}catch(e){if(e?.name!=='AbortError')await copy()}}
function invite(){const b=document.getElementById('mp-invite');if(!b)return;b.style.display='block';b.innerHTML=`<div class="mp-code-label">CODE DE PARTIE</div><div class="mp-code">${MP.code}</div><div class="mp-vbuttons"><button onclick="INSECT_MP.copyInvite()">COPIER</button><button onclick="INSECT_MP.shareInvite()">PARTAGER</button></div>`}
function inject(){if(!enabled())return;const cfg=document.querySelector('#s-menu .menu-config');if(cfg&&!document.getElementById('mp-open-btn')){const w=document.createElement('div');w.style.cssText='margin-top:10px;padding-top:10px;border-top:1px solid rgba(128,80,255,.18)';w.innerHTML='<button id="mp-open-btn" class="btn-play" style="background:linear-gradient(135deg,#123A56,#146A78)" onclick="INSECT_MP.open()">🌐 MULTIJOUEUR</button>';cfg.appendChild(w)}if(document.getElementById('mp-overlay'))return;const e=document.createElement('div');e.id='mp-overlay';e.className='modal-overlay hidden';e.innerHTML=`<div class="modal-box" style="max-width:390px"><div class="modal-header"><div class="modal-title">🌐 MULTIJOUEUR</div><button class="modal-close" onclick="INSECT_MP.close()">✕</button></div><div class="modal-body" style="gap:13px"><div style="font-size:.82rem;color:#9C96C5;line-height:1.5">Jouez contre un inconnu disponible ou défiez directement un ami.</div><button class="btn-primary" style="background:linear-gradient(135deg,#174B39,#19855F)" onclick="INSECT_MP.findOpponent()">🔎 TROUVER UN ADVERSAIRE</button><div id="mp-search" style="text-align:center"></div><div style="text-align:center;color:#5F5A82;font-size:.72rem">— OU DÉFIER UN AMI —</div><button class="btn-primary" onclick="INSECT_MP.create()">⚔️ CRÉER UNE PARTIE PRIVÉE</button><div id="mp-invite" style="display:none;text-align:center;padding:12px;border:1px solid rgba(212,160,23,.28);border-radius:12px"></div><input id="mp-code-input" maxlength="6" placeholder="CODE DE PARTIE" style="height:46px;border-radius:9px;background:#090713;color:#EEE8FF;text-align:center;font-size:1rem;font-weight:800;letter-spacing:.14em"><button class="btn-secondary" onclick="INSECT_MP.join(document.getElementById('mp-code-input').value)">REJOINDRE AVEC UN CODE</button><div id="mp-vote" style="display:none;text-align:center;padding:12px;border:1px solid rgba(128,80,255,.3);border-radius:12px"></div><div id="mp-status" style="min-height:20px;text-align:center;font-size:.76rem"></div></div></div>`;document.body.appendChild(e);const s=document.createElement('style');s.textContent='.mp-vtitle{font-weight:900;color:#CDBBFF;margin-bottom:6px}.mp-vsub,.mp-vstate{font-size:.72rem;color:#8E88B8;line-height:1.4;margin:7px 0}.mp-vbuttons{display:flex;gap:8px;margin-top:9px}.mp-vbuttons button{flex:1;min-height:38px;border:1px solid rgba(128,80,255,.35);border-radius:8px;background:rgba(128,80,255,.12);color:#CDBBFF;font-weight:800}.mp-vbuttons button:disabled{opacity:.4}.mp-result{font-size:.78rem;color:#D4A017;line-height:1.6}.mp-code-label{font-size:.7rem;color:#8E88B8}.mp-code{font-size:1.5rem;color:#D4A017;font-weight:900;letter-spacing:.18em;margin:5px 0}.mp-searching{padding:10px;border:1px solid rgba(40,190,220,.25);border-radius:10px;color:#BCECF3;font-size:.8rem;line-height:1.6}.mp-searching span{font-size:.7rem;color:#7C9FA8}';document.head.appendChild(s)}
function hooks(){if(typeof isHuman==='function'&&!isHuman.__mpWrapped){const o=isHuman;isHuman=function(){if(MP.active&&G&&!G.over)return cur()===MP.localColor;return o.apply(this,arguments)};isHuman.__mpWrapped=true}if(typeof finishTurn==='function'&&!finishTurn.__mpWrapped){const o=finishTurn;finishTurn=function(){const mine=MP.active&&G&&cur()===MP.localColor,r=o.apply(this,arguments);if(mine&&!MP.applyingRemote)queueMicrotask(()=>push());return r};finishTurn.__mpWrapped=true}if(typeof goMenu==='function'&&!goMenu.__mpWrapped){const o=goMenu;goMenu=function(){if(MP.active||MP.code||MP.queueSecret)reset();return o.apply(this,arguments)};goMenu.__mpWrapped=true}}
function autoJoin(){const c=norm(new URLSearchParams(location.search).get('join'));if(c){const i=document.getElementById('mp-code-input');if(i)i.value=c;show();status(`Invitation détectée : ${c}`)}}
Object.assign(MP,{open:show,close:hide,create,join,vote,copyInvite:copy,shareInvite:share,findOpponent,cancelSearch,leave:reset,pushState:push});
function boot(){if(!enabled())return;inject();hooks();autoJoin()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
